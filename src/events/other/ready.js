module.exports = {
    name: "ready", // the event name
    run: async (client) => { // the function that runs the code below
        console.log("The bot is ready and online.") // puts into the console that the bot is online
    }
}